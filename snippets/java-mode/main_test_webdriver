# -*- mode: snippet -*-
# name: WebDriver test bed
# key: psvm_webdriver
# --

// TODO: testing only
public static void main(String[] args) {
    WebDriver driver = Browser.start();
    try {
        $0
    } catch (Throwable e) {
        e.printStackTrace();
    } finally {
        driver.quit();
    }
}