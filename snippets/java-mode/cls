# -*- mode: snippet -*-
# name: new Class
# key: cl
# --
package ${1:example};

public class ${2:`(file-name-base
                    (or (buffer-file-name)
                        (buffer-name)))`} {
  $0
}
